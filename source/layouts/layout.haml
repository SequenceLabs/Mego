!!! 5

- custom_html_tag 'en-GB'

= partial('includes/structure/html-header')

%body{:id=>data.page.body_id}
  %form{:id => "mainform", :action => "/", :method => "post"}
    .wrapper
      = partial('includes/structure/header')
      .container
        = yield
      = partial('includes/structure/footer')
      = partial('includes/structure/javascripts')
